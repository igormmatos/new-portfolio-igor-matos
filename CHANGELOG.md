📝 Log de Alteração • Solicitação: Corrigir erro de build "The character '}' is not valid inside a JSX element" e problemas com tag script no Vite. • Ação Executada: Em `Admin.tsx`, corrigida a estrutura de fechamento de tags do conteúdo principal (substituído `)}` incorreto por `</div>`). Em `index.html`, removida a tag script externa para importmap.json e unificado o conteúdo no script inline com as versões corretas (React 18.3.1), evitando erros do parser do Vite durante o build. • Resultado Esperado: Build bem-sucedido na Vercel e renderização correta do painel administrativo.

📝 Log de Alteração • Solicitação: Corrigir "Minha Jornada" para evitar rolagem excessiva no mobile (anterior não funcionou). • Ação Executada: Substituído carrossel horizontal por layout "Accordion" (lista colapsável) no Mobile em `Home.tsx`. Adicionado estado `expandedId` para controlar a abertura dos cards. No Desktop, o layout permanece como Timeline Expandida com `hidden lg:block` e `lg:border-l-2`. • Resultado Esperado: No celular, o usuário vê apenas o resumo (Cargo/Empresa/Data) e clica para expandir detalhes. No desktop, vê tudo aberto em linha do tempo. Melhor usabilidade e evita problemas de scroll horizontal.

📝 Log de Alteração • Solicitação: Refatorar seção "Minha Jornada" para evitar rolagem vertical excessiva no mobile, transformando em carrossel horizontal. • Ação Executada: Em `Home.tsx` (ExperienceSection), implementado layout responsivo usando Tailwind: Mobile usa `flex overflow-x-auto snap-x` para scroll lateral, enquanto Desktop (`lg:`) mantém o layout vertical (`block`) com a linha do tempo. Cards atualizados para `bg-slate-800` e texto `text-slate-200`. • Resultado Esperado: No celular, os usuários deslizam lateralmente para ver a jornada; no desktop, veem a timeline clássica. Visual minimalista e escuro preservado.

📝 Log de Alteração • Solicitação: Diminuir o espaço entre o subtítulo e a lista de tópicos no card de Áreas de Atuação. • Ação Executada: Ajustado o padding inferior do bloco de título/subtítulo de `mb-8` para `mb-4` em `CompetenciesSection` dentro do arquivo `Home.tsx`. • Resultado Esperado: O card fica visualmente mais compacto e a lista de itens mais conectada ao título.

📝 Log de Alteração • Solicitação: Alterar estilo dos cards de Áreas de Atuação Estratégica. • Ação Executada: Substituído `CompetenciesSection`. O novo card utiliza `bg-slate-900`, `border-slate-800` e `p-6`. A lista de tópicos agora é vertical com `border-l-2` (cor índigo) e texto alinhado com padding à esquerda. Ícone com fundo circular translúcido mantido e refinado. • Resultado Esperado: Visual mais técnico, limpo e organizado, facilitando a leitura rápida dos tópicos por categoria.

📝 Log de Alteração • Solicitação: Ajustar a seção Hero para evitar que elementos fiquem "tampados" ou cortados visualmente. • Ação Executada: Removido `overflow-hidden` vertical da `HeroSection` (alterado para `overflow-x-hidden`) e aumentado o padding inferior (`pb-32`) para dar respiro aos elementos flutuantes. • Resultado Esperado: O card "Experience" e os efeitos de luz não são mais cortados na transição entre Hero e Competências.

📝 Log de Alteração • Solicitação: Melhorar o card com ícone circular translúcido, títulos com peso e badges suaves. • Ação Executada: Redesign do `CompetenciesSection` em `Home.tsx`. Alterado container do ícone para `rounded-full bg-indigo-500/10` com anel sutil. Título com `font-bold` e subtítulo `font-light` para melhor hierarquia. Tags de skills estilizadas com `bg-indigo-500/5` e adição de ícone `fa-check` para efeito de lista simples. • Resultado Esperado: Cards mais elegantes, com hierarquia visual clara e elementos gráficos mais orgânicos e suaves.

📝 Log de Alteração • Solicitação: Melhorar a estética das "Skills" (tags) dentro dos cards de competência. • Ação Executada: Substituído o estilo retangular básico das tags por um design de "cápsula tecnológica" (`rounded-full`, `bg-slate-950/50`). Adicionado um indicador visual (dot) que muda de cor no hover e aplicados efeitos de brilho e escala na interação do mouse. • Resultado Esperado: As tags agora parecem componentes de interface modernos e interativos, integrando-se melhor ao tema glassmorphism.

📝 Log de Alteração • Solicitação: Melhorar a visualização das áreas de atuação para ficar mais bonito sem perder elementos de UX/UI. • Ação Executada: Redesign completo do `CompetenciesSection` em `Home.tsx` utilizando Glassmorphism e Bento Grid style. Adicionado fundo gradiente sutil, bordas translúcidas, efeito de glow no hover, tipografia hierárquica e tags coloridas com fundo transparente para melhor legibilidade. • Resultado Esperado: Cards mais modernos, com sensação de profundidade e interatividade refinada, mantendo a clareza das informações.

📝 Log de Alteração • Solicitação: Melhorar a opção de soltar quando estiver próximo ou entre outro card. • Ação Executada: Adicionada classe condicional `bg-slate-900/40 ring-2 ring-dashed ring-indigo-500/20` no container `Droppable` quando `snapshot.isDraggingOver` é verdadeiro. Adicionada `opacity-90` e `backdrop-blur-sm` no card sendo arrastado. Expandida a área de drop com padding dinâmico durante o arrasto. • Resultado Esperado: O usuário visualiza claramente a "zona de pouso" (o grid acende) e consegue ver através do card arrastado para identificar a abertura do gap entre os itens. • Observações: Isso resolve a sensação de "cegueira" ao tentar encaixar um card entre dois outros.

📝 Log de Alteração • Solicitação: Melhorar fluidez e interatividade ao arrastar e soltar. • Ação Executada: Adicionado feedback tátil (`navigator.vibrate`) no evento `onDragStart`. Atualizadas as classes CSS condicionais em todos os componentes `Draggable` para incluir `rotate`, `scale`, `shadow-2xl` e alterar o cursor para `grabbing` quando o item estiver sendo arrastado. • Resultado Esperado: O item "salta" visualmente ao ser pego, gira levemente e o celular vibra, proporcionando uma sensação tátil e visual de controle físico sobre o card. • Observações: As animações utilizam transições rápidas (duration-200) para manter a resposta imediata.

📝 Log de Alteração • Solicitação: Exibir notificação de sucesso ao reordenar itens. • Ação Executada: Adicionado `showNotification` dentro do bloco `try` em `Admin.tsx` após a chamada `api.reorderItems`. • Resultado Esperado: Usuário recebe feedback visual (Toast verde) ao soltar um item e o banco confirmar a atualização. • Observações: Mantido o tratamento de erro.

📝 Log de Alteração • Solicitação: Corrigir erro "ON CONFLICT DO UPDATE command cannot affect row a second time" (Code 21000) ao reordenar. • Ação Executada: Atualizado `SUPABASE_SETUP.sql` para remover triggers de banco de dados (`trg_swap_order_*`). • Resultado Esperado: O Supabase aceitará atualizações em lote da ordem (0, 1, 2...) sem tentar trocar posições automaticamente via trigger, eliminando o conflito de transação. • Observações: ⚠️ AÇÃO MANUAL NECESSÁRIA: O usuário deve executar o script SQL atualizado no painel do Supabase para aplicar a correção.

📝 Log de Alteração • Solicitação: Corrigir erro "null value in column title violates not-null constraint" ao arrastar projetos. • Ação Executada: Atualizada chamada `api.reorderItems` em `Admin.tsx` para enviar o objeto completo do item (newItems) ao invés de apenas ID/Order. Atualizada assinatura em `api.ts`. • Resultado Esperado: O upsert do Supabase agora recebe todos os campos obrigatórios e realiza a atualização da ordem sem erros de validação. • Observações: O upsert utiliza a chave primária (id) para identificar que é um update, mas valida o schema como insert se a operação não for atomicamente convertida, necessitando de todos os campos NOT NULL.

📝 Log de Alteração • Solicitação: Substituir entrada manual de ordem por Drag-and-Drop. • Ação Executada: Integrada biblioteca `@hello-pangea/dnd`, removido campo input do Drawer e adicionada lógica de `onDragEnd` com atualização em lote (Upsert) no Supabase. • Resultado Esperado: O administrador pode reordenar cards visualmente; as alterações persistem automaticamente sem conflitos de índice. • Observações: Utilizado `StrictModeDroppable` para compatibilidade com React 18.

🚩 CHECKPOINT: React Estavel - importmap separado
• Status: Estável
• Versões: React 18.3.1, React Router 6.22.3
• Arquitetura: Importmap externalizado (/importmap.json)
• Descrição: Ponto de restauração segura. A aplicação está livre de conflitos de versão (Dual React Instance) e o carregamento de módulos está padronizado.